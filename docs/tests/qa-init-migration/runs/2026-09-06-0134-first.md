# Init CLI QA: first execution

- Date: 2026-09-06, approximately 01:34 CEST (JSON timestamps use UTC).
- Procedure: [CLI QA](../README.md); executable: checkout `.venv/bin/python -m loadout`.
- Revision: `ff9e7e8b268e456fa127a07da8848bc004e41141`, with Task7 production changes.
- Production fingerprint: `38c53e7e5726b7c138eada670307ee4db6acf16ddcdf7218aac353e5209da278`.
- Driver fingerprint: `38f0d3960b8d890bf4d5d8e5b5c224cf13907010b751387244a34ec08382d6d6`.
- Environment: macOS arm64, Python 3.13, disposable Git repositories and existing fake-home fixtures.

Command, run from the development checkout:

```sh
LOADOUT_QA_REPORT_DIR=/var/folders/v_/dg_p8zcs2nlcg2xq_v834dlh0000gn/T/loadout-init-qa.SsuHN2bxon uv run pytest -q tests/manual_init_qa.py
```

Result: **3 failed, 9 passed in 45.29 seconds**, exit 1, no skips. One passing case only
prepared a host fixture; it did not invoke an agent. Full command records are in the
[first-run evidence directory](2026-09-06-0134-first/).

## Findings

| Case | Observation | Diagnosis |
|---|---|---|
| Global conflicting copies | Selection exited 2 with `source-mapping` and `invalid-source-selection` | The driver had not mapped its authored `claude/` directory, so it never created the intended two-candidate conflict |
| Held failing pre-commit hook | Pending state observed; CLI returned `interrupted`, journal and recovery commands, exit 1 | Driver incorrectly expected exit 3; the documented interrupted-transaction exit is 1 |
| Post-commit concurrent change | Checkpoint completed; foreign source creation was detected; CLI returned `interrupted`, exit 1 | Same incorrect driver exit expectation prevented the subsequent resume check |

These are driver defects, not evidence of a product regression. Corrections added an explicit
mapping and asserted the `source-conflict` code, and used the documented interruption exit.
No product source changed for these findings. The recovery/resume portions were **not reached**
in this execution; their successful outcomes cannot be inferred from the error messages.

The other runtime cases passed: empty project, populated project with partial staging, global
runtime fields plus skill lifecycle, all three starter choices, and both PTY cancellations.
The driver also prepared an installed-skill fixture successfully.

## Retest and cleanup

The [complete retest](2026-09-06-0136-retest.md) preserves these failures and reports fresh results.
The failing pre-commit hook was explicitly released and its child exited. The two fixture-owned
hooks left by failed assertions were subsequently removed; failed repository/journal state was
retained for inspection. No real agent destinations or development-checkout Git hooks changed.

Metadata correction: the empty-project record names Q07 as well as Q01, but its fixture already
had a Git repository. It proves Q01, not new-repository creation. The driver now labels it Q01 only.
