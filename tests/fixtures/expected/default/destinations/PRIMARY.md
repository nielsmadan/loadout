<!-- GENERATED by loadout — do not edit. Edit the fragments in instructions/ and re-run `loadout sync`. -->

# Intro

A plain fragment. Its only job is to be first in the composed document, so the
generated-by header and the blank-line separator that follows it are both visible
in the expected output.

## Policy

The default variant. Its sibling `policy.variant.md` replaces it under another
profile, which is how a profile swaps one fragment without touching the rest.

## Shared

A fragment more than one target composes, so the expected files prove one source
fragment reaches several documents unchanged.

## Closing

The last fragment, so the expected output pins the single trailing newline that
`render` appends after the final block.
