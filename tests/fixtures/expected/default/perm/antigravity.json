{
  "permissions": {
    "allow": [
      "command(alpha)",
      "command(beta sub)",
      "command(zeta)",
      "command(eta)",
      "mcp(svc/*)",
      "mcp(svc.two/read)"
    ],
    "deny": [
      "command(zeta)",
      "command(iota push)",
      "mcp(blocked/*)",
      "mcp(svc/purge)"
    ],
    "ask": [
      "command(theta check)",
      "mcp(svc/write)"
    ]
  }
}
