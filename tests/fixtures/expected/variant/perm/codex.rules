# GENERATED by loadout — do not edit.
# Edit permissions.toml and re-run `loadout sync`.
#
# Read-only commands are allowed; remote-publishing and destructive
# ops are forbidden; `ask` commands prompt. Glob entries are skipped
# (see end of file) — Codex's token matcher can't express them.

prefix_rule(pattern = ["alpha"], decision = "allow")
prefix_rule(pattern = ["beta", "sub"], decision = "allow")
prefix_rule(pattern = ["eta"], decision = "allow")

prefix_rule(pattern = ["zeta"], decision = "forbidden")
prefix_rule(pattern = ["iota", "push"], decision = "forbidden")

prefix_rule(pattern = ["theta", "check"], decision = "prompt")

# Skipped — glob entries Codex's token matcher can't express
# (these fall through to Codex's normal approval prompt):
#   gamma-*
#   delta run --tag=*
