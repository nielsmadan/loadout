# Template block

Contributed by the `web` template as one unnamed block. It must appear before
anything the project itself declares, so that a project fragment saying the
opposite wins by being read last.
